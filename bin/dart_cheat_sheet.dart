import 'package:dart_cheat_sheet/dart_cheat_sheet.dart';

void main(List<String> arguments) {
  // basic
  Basic basicCS = Basic();
  basicCS.useDebug();
  basicCS.controlFlow('use-loops');
  basicCS.annonymousFunc();
}
